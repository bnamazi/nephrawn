import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without errors', (WidgetTester tester) async {
    // TODO: Add proper widget tests with provider mocking
    expect(true, isTrue);
  });
}
